import { HRC20 } from '../contracts';
import { BridgeResponse, HRC20Info, ITransactionOptions } from '../interfaces';
import { BridgeToken } from './bridgeToken';
export declare class BridgeHRC20Token extends BridgeToken {
    private getContractAddresses;
    getBridgedTokenAddress(token: HRC20, txOptions?: ITransactionOptions): Promise<string>;
    ethToHmy(sender: string, recipient: string, token: HRC20, tokenInfo: HRC20Info): Promise<BridgeResponse>;
    hmyToEth(sender: string, recipient: string, token: HRC20, tokenInfo: HRC20Info, txOptions?: ITransactionOptions): Promise<BridgeResponse>;
}
//# sourceMappingURL=bridgeHrc20Token.d.ts.map