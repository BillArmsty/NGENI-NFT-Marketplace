import { ContractOptions } from '@harmony-js/contract/dist/utils/options';
import { Transaction } from '@harmony-js/transaction';
import { BaseContract } from '../../contracts';
import { BNish, ContractProviderType, ITransactionOptions } from '../../interfaces';
export declare class HRC1155HmyManager extends BaseContract {
    constructor(address: string, provider: ContractProviderType, options?: ContractOptions);
    lockHRC1155Token(ethTokenAddr: string, tokenId: BNish, recipient: string, amount: BNish, data: string[], txOptions?: ITransactionOptions): Promise<Transaction>;
    lockHRC1155Tokens(ethTokenAddr: string, tokenIds: BNish[], recipient: string, amounts: BNish[], data: string[], txOptions?: ITransactionOptions): Promise<Transaction>;
    unlockHRC1155Token(ethTokenAddr: string, tokenId: BNish, amount: BNish, recipient: string, receiptId: string, data: string[], txOptions?: ITransactionOptions): Promise<Transaction>;
    unlockHRC1155Tokens(ethTokenAddr: string, tokenIds: BNish[], amounts: BNish[], recipient: string, receiptId: string, data: string[], txOptions?: ITransactionOptions): Promise<Transaction>;
}
//# sourceMappingURL=index.d.ts.map