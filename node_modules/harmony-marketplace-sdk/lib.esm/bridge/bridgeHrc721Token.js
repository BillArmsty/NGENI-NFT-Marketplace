var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
import { TxStatus } from '@harmony-js/transaction';
import { hexToNumber } from '@harmony-js/utils';
import BN from 'bn.js';
import { AddressZero, DEFAULT_TX_OPTIONS, DEVNET_HRC721_CONTRACTS_ADDRESSES, HARMONY_RPC_WS, MAINNET_HRC721_CONTRACTS_ADDRESSES, } from '../constants';
import { waitForNewBlock } from '../utils';
import { BridgeToken } from './bridgeToken';
import { BridgedHRC721Token } from './bridgedHrc721Token';
import { HRC721EthManager } from './hrc721EthManager';
import { HRC721HmyManager } from './hrc721HmyManager';
import { HRC721TokenManager } from './hrc721TokenManager';
export class BridgeHRC721Token extends BridgeToken {
    getContractAddresses() {
        return this.isMainnet ? MAINNET_HRC721_CONTRACTS_ADDRESSES : DEVNET_HRC721_CONTRACTS_ADDRESSES;
    }
    getBridgedTokenAddress(token, tokenId, txOptions = DEFAULT_TX_OPTIONS) {
        return __awaiter(this, void 0, void 0, function* () {
            const { ethManagerAddress, tokenManagerAddress } = this.getContractAddresses();
            const ethManager = new HRC721EthManager(ethManagerAddress, this.ethOwnerWallet);
            const tokenManager = new HRC721TokenManager(tokenManagerAddress, this.ethMasterWallet);
            let erc721Addr = undefined;
            try {
                erc721Addr = yield ethManager.mappings(token.address);
            }
            catch (err) { }
            if (!erc721Addr || erc721Addr === AddressZero) {
                const [name, symbol, tokenURI] = yield Promise.all([
                    token.name(txOptions),
                    token.symbol(txOptions),
                    token.tokenURI(tokenId, txOptions),
                ]);
                yield ethManager.addToken(tokenManager.address, token.address, name, symbol, tokenURI);
                erc721Addr = yield ethManager.mappings(token.address);
            }
            return erc721Addr;
        });
    }
    ethToHmy(sender, recipient, token, tokenInfo) {
        return __awaiter(this, void 0, void 0, function* () {
            const { ethManagerAddress } = this.getContractAddresses();
            const ethManager = new HRC721EthManager(ethManagerAddress, this.ethOwnerWallet);
            const erc721Address = yield ethManager.mappings(token.address);
            const erc721 = new BridgedHRC721Token(erc721Address, this.ethOwnerWallet);
            const balance = yield erc721.balanceOf(sender);
            if (balance.lt(1)) {
                throw Error('Insufficient funds');
            }
            const { tokenId } = tokenInfo;
            if (!tokenId) {
                throw Error('Error in tokenInfo, tokenId cannot be undefined for HRC721');
            }
            const approveTx = yield erc721.approve(ethManager.address, tokenId);
            if ((approveTx === null || approveTx === void 0 ? void 0 : approveTx.status) !== 1) {
                throw new Error(`Failed to approve erc721: ${approveTx === null || approveTx === void 0 ? void 0 : approveTx.transactionHash}`);
            }
            const burnTx = yield ethManager.burnToken(erc721Address, tokenId, recipient);
            if ((burnTx === null || burnTx === void 0 ? void 0 : burnTx.status) !== 1) {
                throw new Error(`Failed to approve erc721: ${burnTx === null || burnTx === void 0 ? void 0 : burnTx.transactionHash}`);
            }
            return { addr: token.address, receiptId: burnTx === null || burnTx === void 0 ? void 0 : burnTx.transactionHash };
        });
    }
    hmyToEth(sender, recipient, token, tokenInfo, txOptions = DEFAULT_TX_OPTIONS) {
        var _a, _b;
        return __awaiter(this, void 0, void 0, function* () {
            const { tokenId, ws = HARMONY_RPC_WS, waitingFor = 12 } = tokenInfo;
            if (!tokenId) {
                throw Error('Error in tokenInfo, tokenId cannot be undefined for HRC721');
            }
            const balance = yield token.balanceOf(sender, txOptions);
            if (balance.lt(new BN(1))) {
                throw new Error(`Insufficient funds. Balance: ${balance}. TokenId: ${tokenId}`);
            }
            const { hmyManagerAddress } = this.getContractAddresses();
            const hmyManager = new HRC721HmyManager(hmyManagerAddress, this.hmyOwnerWallet);
            const erc721Addr = yield this.getBridgedTokenAddress(token, tokenId, txOptions);
            const approveTx = yield token.approve(hmyManagerAddress, tokenId, txOptions);
            if ((approveTx === null || approveTx === void 0 ? void 0 : approveTx.txStatus) !== TxStatus.CONFIRMED) {
                throw new Error(`Failed to approve manager: ${approveTx === null || approveTx === void 0 ? void 0 : approveTx.id}`);
            }
            const lockTokenTx = yield hmyManager.lockNFT721Token(token.address, tokenId, recipient, txOptions);
            if ((lockTokenTx === null || lockTokenTx === void 0 ? void 0 : lockTokenTx.txStatus) !== TxStatus.CONFIRMED) {
                throw new Error(`Failed to lock tokens: ${lockTokenTx === null || lockTokenTx === void 0 ? void 0 : lockTokenTx.id}`);
            }
            const expectedBlockNumber = parseInt(hexToNumber((_b = (_a = lockTokenTx === null || lockTokenTx === void 0 ? void 0 : lockTokenTx.receipt) === null || _a === void 0 ? void 0 : _a.blockNumber) !== null && _b !== void 0 ? _b : ''), 10) + waitingFor;
            yield waitForNewBlock(expectedBlockNumber, ws, token.messenger.chainType, token.messenger.chainId);
            return { addr: erc721Addr, receiptId: lockTokenTx.id };
        });
    }
}
//# sourceMappingURL=bridgeHrc721Token.js.map