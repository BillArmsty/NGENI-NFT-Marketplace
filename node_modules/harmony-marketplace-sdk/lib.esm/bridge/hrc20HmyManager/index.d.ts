import { ContractOptions } from '@harmony-js/contract/dist/utils/options';
import { Transaction } from '@harmony-js/transaction';
import { BaseContract } from '../../contracts';
import { BNish, ContractProviderType, ITransactionOptions } from '../../interfaces';
export declare class HRC20HmyManager extends BaseContract {
    constructor(address: string, provider: ContractProviderType, options?: ContractOptions);
    lockToken(hmyTokenAddr: string, amount: BNish, recipient: string, txOptions?: ITransactionOptions): Promise<Transaction>;
    lockTokenFor(hmyTokenAddr: string, userAddr: string, amount: BNish, recipient: string, txOptions?: ITransactionOptions): Promise<Transaction>;
    unlockToken(hmyTokenAddr: string, amount: BNish, recipient: string, receiptId: string, txOptions?: ITransactionOptions): Promise<Transaction>;
    lockOne(amount: BNish, recipient: string, txOptions?: ITransactionOptions): Promise<Transaction>;
    unlockOne(amount: BNish, recipient: string, receiptId: string, txOptions?: ITransactionOptions): Promise<Transaction>;
}
//# sourceMappingURL=index.d.ts.map