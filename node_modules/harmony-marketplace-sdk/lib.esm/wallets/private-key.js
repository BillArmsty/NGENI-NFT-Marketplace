import { Key } from './key';
export class PrivateKey extends Key {
    constructor(url, privateKey, chainId, chainType) {
        super(url, chainId, chainType);
        this.addByPrivateKey(privateKey);
    }
}
//# sourceMappingURL=private-key.js.map